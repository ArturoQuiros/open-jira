import { Typography } from "@mui/material";
import type { NextPage } from "next";

const HomePage: NextPage = () => {
  return <Typography variant="h1">Hola Mundo</Typography>;
};

export default HomePage;
